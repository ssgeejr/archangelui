<project xmlns="http://maven.apache.org/POM/4.0.0"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
	<modelVersion>4.0.0</modelVersion>

	<!-- <parent> <groupId>org.ioexcept</groupId> <artifactId>ArchAngel</artifactId> 
		<version>1.0-SNAPSHOT</version> </parent> <artifactId>ArchAngelFrontEnd</artifactId> 
		<packaging>war</packaging> -->

	<groupId>org.ioexcept</groupId>
	<artifactId>ArchAngelFrontEnd</artifactId>
	<version>1.0-SNAPSHOT</version>
	<packaging>war</packaging>

	<properties>
		<project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
		<maven.compiler.source>1.8</maven.compiler.source>
		<maven.compiler.target>1.8</maven.compiler.target>
		<tomcat7-groupId>org.apache.tomcat.maven</tomcat7-groupId>
		<tomcat7-artifactId>tomcat7-maven-plugin</tomcat7-artifactId>
		<tomcat7-version>2.1</tomcat7-version>
		<tomcat8-groupId>org.apache.tomcat.maven</tomcat8-groupId>
		<tomcat8-artifactId>tomcat8-maven-plugin</tomcat8-artifactId>
		<tomcat8-version>3.0-r1756463</tomcat8-version>
	</properties>

	<build>
		<finalName>archAngelFrontEnd</finalName>
		<plugins>
			<plugin>
				<artifactId>maven-compiler-plugin</artifactId>
				<version>3.1</version>
				<configuration>
					<source>${maven.compiler.source}</source>
					<target>${maven.compiler.target}</target>
					<fork>true</fork>
					<!--<executable>C:\Program Files\Java\jdk1.8.0_161\bin\javac</executable> -->
				</configuration>
			</plugin>
			<!-- <plugin> <artifactId>maven-war-plugin</artifactId> <version>2.4</version> 
				<configuration> <warSourceDirectory>WebContent</warSourceDirectory> <failOnMissingWebXml>false</failOnMissingWebXml> 
				</configuration> </plugin> -->
			<!-- <plugin> <groupId>org.eclipse.jetty</groupId> <artifactId>jetty-maven-plugin</artifactId> 
				<version>9.4.10.v20180503</version> </plugin> -->
			<plugin>
				<groupId>${tomcat7-groupId}</groupId>
				<artifactId>${tomcat7-artifactId}</artifactId>
				<version>${tomcat7-version}</version>
				<executions>
					<execution>
						<id>tomcat-run</id>
						<goals>
							<goal>exec-war-only</goal>
						</goals>
						<phase>package</phase>
						<configuration>
							<path>/</path>
							<enableNaming>false</enableNaming>
							<buildDirectory>${project.basedir}/docker</buildDirectory>
							<finalName>archAngelFrontEnd.jar</finalName>
							<charset>utf-8</charset>
						</configuration>
					</execution>
				</executions>
			</plugin>
		</plugins>
	</build>

	<dependencies>
		<dependency>
			<groupId>junit</groupId>
			<artifactId>junit</artifactId>
			<version>4.11</version>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>javax.servlet</groupId>
			<artifactId>servlet-api</artifactId>
			<version>2.5</version>
			<scope>provided</scope>
		</dependency>

	</dependencies>
</project>
